<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>URL - TinyApp Example</title>
</head>

<body>

  <%- include('partials/_header') %>
  
  <div class="card text-center">
      <div class="card-header"></div>
      <div class="card-body">
        <% if (Object.keys(urls).length >= 1) {%>

          <h5 class="card-title">TinyURL for: <%= longURL %></h5>
          <p class="card-text">Short URL: <a href="/u/<%= shortURL %>"><%= shortURL %></a></p>
          <p class="card-text"> Long URL: <a href="/u/<%= longURL %>"><%= longURL %></a></p>
          <form method="POST" action="/urls/<%= shortURL %>">
            <label for="newLong">Update URL:</label>
            <input id="newLong" type="text" name="longURL"/>
            <button type = "submit">Update</button>
          </form>
          <a href="/urls/new">Shorten a new URL</a>
          <% } else if (Object.keys(urls).length < 1 && user === undefined) {%>
          <h5>Please log in or register to view your URL's</h5>
          <% } else if (user.id && Object.keys(urls).length < 1) {%>
          <h5>This user is not authorized to access this URL</h5>
          <% } %>
      </div>
    <div class="card-footer text-muted"></div>
  </div>
  <!-- Bootstrap JS -->
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
</body>
</html>
